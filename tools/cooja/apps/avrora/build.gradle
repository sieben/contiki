apply plugin: "java"

repositories {
    mavenCentral()
}

dependencies {
    compile project(":")
    compile files("lib/avrora-small.jar")
    compile 'log4j:log4j:1.2.16'
    compile 'org.jdom:jdom:1.1'
}
